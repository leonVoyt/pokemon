export const useAllTypes = ({ arr }) => {
  if (arr) {
  }
  // let res = arr.map((el) => {
  //   el.type.forEach((e) => {
  //     return e.type.name
  //   })
  // })
  console.log(arr)

  return arr
}
